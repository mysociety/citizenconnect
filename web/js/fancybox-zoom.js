(function($) {
    $(function() {
        // Enable fancy box zoom on the images
        $("a.fancybox-zoom").fancybox({
          // Defaults seem fine for our purposes
        });
    });
})(window.jQuery);
